server:  port: ${vcap.application.port:8761}spring:  profiles.active: local 
  application:    name: cts-eureka    eureka:  server:    enable-self-preservation: false  client:    register-with-eureka: false    fetch-registry: false    #service-url:      #default-zone : http://${vcap.services.cloudProps.credentials.config-uri:localhost:8761}/eureka/  #instance:    #hostname: localhost info:  app:    name : CTS NT eureka Server    descriotion : This is test eureka server as part of training         security:  basic.enabled: false     #---#spring:   #profiles:  cloud  #eureka:  #instance:    #hostname: ${vcap.application.application_uris[0]:localhost}    #non-secure-port: 80  